# Tested in the separate Python 3.11 tree_mask2former environment.
# Install a CUDA build of torch/torchvision supporting the GTX 1080 Ti (sm_61).
-r requirements.txt
transformers==5.17.0
pycocotools==2.0.11
scipy==1.17.1
torch>=2.6
torchvision>=0.21
safetensors>=0.4
